export const metadata = {
  title: 'Editor de cotizaciones',
  description: 'Build and customize your quote',
};

export default function QuoteBuilderLayout({ children }: { children: React.ReactNode }) {
  return children;
}
